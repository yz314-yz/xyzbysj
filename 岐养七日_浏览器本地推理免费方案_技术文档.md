# 毕业设计技术方案 —《岐养七日》

**AI 中医识图与七日养生系统**

"方案二 + 方案三"整合版：免费静态部署、浏览器本地识图、规则化七日建议、可选 AI 文本增强

> **设计目标**
>
> 实现一个可公开访问、无需租用云 GPU、默认不占用开发者显卡的毕业设计演示系统。图片在访问者浏览器内处理，基础功能依靠轻量视觉模型与规则引擎完成；高性能设备可选择加载本地小型文本模型，提升报告表达。

| 不租云 GPU | 不占自己的 GPU | 不使用付费 API |
|---|---|---|
| 公网不部署推理服务器 | 默认不从你的电脑执行推理 | 基础链路不依赖外部模型接口 |

**适用范围：** 毕业设计答辩、小范围体验、原型展示。系统定位为健康管理和养生学习辅助，不用于疾病确诊、处方或替代就医。

版本：v1.0 | 日期：2026 年 7 月 | 文档性质：技术设计说明

---

## 1. 核心结论与边界

**可行结论：** "方案二（浏览器本地运行）+ 方案三（轻量视觉模型 + 规则引擎）"可以结合，而且最适合当前毕业设计的免费公网演示目标。

> **重要现实：** 如果你不使用自己的 GPU、不租云 GPU、不调用付费 API，那么推理必须运行在访问者自己的设备上。浏览器本地推理正是把算力放到访问者的 CPU / GPU，而不是开发者或云服务器。

**本方案将系统拆为三层：**

- **基础识图层（默认）：** 在浏览器中运行轻量视觉模型，完成图片质量检测、兴趣区域处理与可观察特征提取。
- **养生决策层（默认）：** 将结构化特征交给规则引擎，生成可解释的七日生活方式建议。
- **AI 文本增强层（可选）：** 仅在访问者设备支持 WebGPU、性能足够时加载量化文本模型，将结构化结论润色成更自然的报告。

### 1.1 不建议的默认方案

不要把 Qwen2.5-VL-3B-Instruct 直接作为"所有公网访客的浏览器默认模型"。它是 3B 级视觉语言模型，虽可在本地 Python/Transformers 环境处理图像，但首次模型下载、浏览器显存、兼容性与加载时间会显著影响普通用户体验。更适合把它设为答辩电脑或高配设备上的"本地深度演示模式"。

---

## 2. 推荐总体架构

**部署原则：** 前端使用静态站点部署；不部署后端推理服务；默认不上传原始图片；模型计算在浏览器内部完成。

![图 1 浏览器本地推理的默认执行路径](media/image1.png)

### 2.1 数据流

| 01 | 02 | 03 | 04 | 05 |
|---|---|---|---|---|
| 用户选择图片 | 本地质量检测 | 轻量模型提取特征 | 规则生成计划 | 展示报告 / 可选润色 |

**关键点：** 默认路径中，图片、特征 JSON 和报告都在访问者浏览器内生成。除非以后另行加入账号、历史记录或远程服务，原始图片不需要上传到你的服务器。

---

## 3. 功能模块与技术选型

| 模块 | 推荐实现 | 运行位置 | 是否默认开启 | 说明 |
|---|---|---|---|---|
| 前端页面 | Vite / React 或现有前端项目 | GitHub Pages | 是 | 静态页面、上传、结果展示与交互。 |
| 图片预处理 | Canvas API + 浏览器本地算法 | 用户浏览器 | 是 | 压缩、方向纠正、清晰度 / 光照提示。 |
| 轻量视觉推理 | ONNX Runtime Web | 用户浏览器 | 是 | 优先 WebGPU；不支持时使用 WASM / CPU 兜底。 |
| 规则引擎 | TypeScript / JavaScript 规则表 | 用户浏览器 | 是 | 根据特征 JSON 生成可解释的七日建议。 |
| 文本润色 | Transformers.js + 量化文本模型 | 用户浏览器 | 否 | 只在性能检测通过后启用，避免拖慢基础体验。 |
| 深度演示 | Qwen2.5-VL-3B-Instruct 本地 Python 环境 | 答辩电脑或高配设备 | 否 | 用于展示多模态增强能力，不作为公网默认链路。 |

### 3.1 WebGPU 与兼容性策略

**WebGPU 的作用：** 让浏览器直接使用访问者设备的 GPU 加速模型推理。若设备或浏览器不支持 WebGPU，系统不能报错退出，必须自动切换到 WASM / CPU 的基础模式。

| 优先路径 | 兜底路径 | 不满足条件 |
|---|---|---|
| **WebGPU** — 高性能电脑：加载更快，可启用 AI 文本增强。 | **WASM / CPU** — 所有兼容浏览器：只保留基础识图与规则报告。 | **提示用户** — 清晰提示更换设备、降低图片分辨率或使用基础模式。 |

---

## 4. 识图、特征 JSON 与规则引擎设计

**不要让通用图像模型"直接看图并确诊"。** 正确的毕业设计表述是：轻量模型负责提取"可观察的图像特征"，规则引擎依据特征生成健康管理与养生建议。这样更可解释、也更容易测试与答辩。

### 4.1 建议的基础识图流程

> **A. 图片质量检测：** 检测是否过暗、过曝、严重模糊、角度不正确或主体过小；不合格时仅提示重新拍摄。
>
> **B. 感兴趣区域处理：** 舌象、面部、手部采用不同入口与裁剪 / 遮罩逻辑，避免一个模型承担所有任务。
>
> **C. 轻量特征提取：** 输出颜色倾向、纹理等级、舌苔覆盖程度、裂纹明显程度等"可观察描述"，不输出疾病结论。
>
> **D. 置信与拒答：** 置信度过低或拍摄不规范时，不强行解释；展示"无法可靠分析，请重新拍摄"。
>
> **E. 规则映射：** 按特征组合、用户选择的作息偏好和禁忌信息，生成七日生活方式建议。

### 4.2 特征 JSON 示例

```json
{
  "imageQuality": "good",
  "analysisScope": "tongue",
  "observedFeatures": {
    "colorTone": "淡红倾向",
    "coatingLevel": "中等",
    "crackLevel": "轻微"
  },
  "confidence": 0.78,
  "safetyGate": "pass"
}
```

**说明：** 字段必须围绕"可观察特征"设计；不要输出"确诊""患有某疾病""处方"等医疗结论。

### 4.3 七日建议规则的最小实现

| 输入条件 | 输出类型 | 页面展示示例 |
|---|---|---|
| 图片质量不足 | 重新拍摄提示 | "当前照片光线或清晰度不足，建议在自然光下重新拍摄。" |
| 可观察特征组合 A | 作息建议 | "未来 7 天优先保证规律睡眠，减少连续熬夜。" |
| 可观察特征组合 B | 饮食与饮水提醒 | "建议以清淡、规律饮食为主；如有不适应及时咨询专业人员。" |
| 任何风险提示 | 免责声明 | "本结果仅用于健康管理与学习参考，不构成医疗诊断或治疗建议。" |

---

## 5. Qwen2.5-VL-3B-Instruct 在项目中的正确位置

**定位建议：** 把 Qwen2.5-VL-3B-Instruct 放在"本地答辩增强模式"，不要放在"公网默认浏览器模式"。

| 公网基础模式 | 答辩 / 高配本地增强模式 |
|---|---|
| 轻量 ONNX 视觉模型 | 本机 Python + Transformers 环境 |
| 规则引擎生成七日建议 | Qwen2.5-VL-3B-Instruct 可做多模态解释演示 |
| 浏览器 WebGPU / WASM 自适应 | 使用安装该模型的设备算力 |
| 普通设备仍可使用 | 不对普通公网用户强制开放 |

**许可证提醒：** Qwen2.5-VL-3B-Instruct 模型仓库当前附带 Qwen Research License，明确授予非商业研究或评估用途的许可；若未来项目用于商业服务，应先向权利方确认或申请适当许可。毕业设计演示应在项目说明中标注模型来源与许可证。

---

## 6. 免费部署与资源管理

**推荐部署：** 使用 GitHub Pages 托管前端静态文件。GitHub Pages 适合发布 HTML、CSS、JavaScript 等静态站点；它不支持 PHP、Python 等服务端语言，因此本方案本来就不依赖服务端推理。

> **模型文件管理限制：** GitHub Pages 对站点大小、带宽有使用限制，模型权重不宜无节制直接塞进普通站点仓库。开发阶段可使用体积较小的 ONNX 模型；较大的可选模型应采用独立、合规的静态文件托管方式，并在页面上明确告知首次下载体积与设备要求。

### 6.1 部署目录建议

```
project/
├─ src/                  # 页面、状态管理、规则引擎
├─ public/models/        # 小型 ONNX 模型（仅放可接受体积的基础模型）
├─ public/assets/        # 图标、背景、说明素材
├─ src/inference/        # ONNX Runtime Web、WebGPU / WASM 切换
├─ src/rules/            # 特征映射与七日计划规则
├─ src/privacy/          # 本地处理、清理缓存、免责声明
├─ scripts/              # 模型转换与校验脚本
└─ .github/workflows/    # GitHub Pages 构建部署工作流
```

### 6.2 图片与隐私策略

- 默认不上传原始图片；图片仅用于当前浏览器会话内的推理。
- 不要在前端代码中放 API Key，也不要因为"零成本"而接入未受控的公共推理接口。
- 页面明确提示：关闭页面后图片不会保存；如启用本地缓存，应提供"清除本地模型与缓存"按钮。
- 面部、舌象、手部图片都可能包含敏感个人信息；答辩演示优先使用经同意的样例图或合成 / 公开许可素材。

---

## 7. 开发路线与验收标准

| 阶段 | 目标 | 主要产出 | 验收点 |
|---|---|---|---|
| 阶段 1：先跑通界面 | 完成上传、模式选择、报告页 | 使用模拟 JSON 与规则报告 | 不接模型也可完整演示流程。 |
| 阶段 2：接入基础识图 | 完成图片质量检测与一类特征提取 | ONNX Runtime Web 推理模块 | 浏览器本地可运行；不支持 WebGPU 时有 CPU 兜底。 |
| 阶段 3：完善规则引擎 | 生成七日养生计划 | 可配置规则表、免责声明、拒答逻辑 | 报告可解释；不会输出诊断或处方。 |
| 阶段 4：可选 AI 增强 | 提升文本表达与答辩展示力 | 文本润色模型 / Qwen 本地演示模式 | 只在高配设备开启；基础模式不受影响。 |

### 7.1 关键验收清单

- [ ] 关闭联网或拦截网络请求后，已下载模型的基础识图仍可在浏览器本地工作。
- [ ] 浏览器不支持 WebGPU 时，不报错；自动落到基础 CPU / WASM 路径。
- [ ] 拍摄质量不合格时，系统优先要求重拍，而非生成看似确定的分析。
- [ ] 报告中只出现可观察特征、生活方式提醒与免责声明，不出现疾病确诊、用药、剂量、处方。
- [ ] 用户图片不传至自建服务器；开发者设备 GPU 不参与公网用户推理。
- [ ] 答辩电脑可单独展示本地 Qwen2.5-VL-3B-Instruct 增强分析，但不影响基础模式。

---

## 8. 答辩可直接使用的项目表述

> 以下为答辩演示用的表述模板，使用时请完整朗读，不要修改边界声明内容。

> "本系统采用浏览器本地推理：轻量视觉模型在用户浏览器内提取可观察特征，规则引擎生成七日养生建议。因此基础模式不依赖开发者 GPU、云端 GPU 或付费 API。支持 WebGPU 的设备可启用文本润色；答辩电脑可展示本机 Qwen2.5-VL-3B-Instruct 增强模块。系统仅用于中医养生学习与健康管理辅助，不提供确诊、处方或替代就医结论。"

**建议现场演示顺序：** ① 体验网页并上传样例图 → ② 展示本地模型加载与 WebGPU / CPU 状态 → ③ 查看特征 JSON → ④ 查看规则生成的七日计划 → ⑤ 在答辩电脑展示 Qwen 增强模式。

---

## 9. 官方资料与许可证核对

- [Qwen/Qwen2.5-VL-3B-Instruct 模型卡与本地 Transformers 使用示例](https://huggingface.co/Qwen/Qwen2.5-VL-3B-Instruct)
- [Qwen Research License（非商业研究 / 评估用途条款）](https://huggingface.co/Qwen/Qwen2.5-VL-3B-Instruct/blob/main/LICENSE)
- [Hugging Face Transformers.js：浏览器中本地运行模型](https://huggingface.co/docs/transformers.js/index)
- [Transformers.js：WebGPU 加速说明](https://huggingface.co/docs/transformers.js/guides/webgpu)
- [ONNX Runtime Web：JavaScript / 浏览器推理与兼容性](https://onnxruntime.ai/docs/get-started/with-javascript/web.html)
- [GitHub Pages：创建静态站点](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)
- [GitHub Pages：站点大小、带宽与用途限制](https://docs.github.com/en/pages/getting-started-with-github-pages/github-pages-limits)

---

## 10. AI 提示词限制与边界规范

> 以下为本项目后端实际使用的 AI 系统提示词及其边界限制要求。提示词必须严格包含边界声明，确保 AI 输出不越界、不影响系统其他模块。

### 10.1 当前提示词位置

| 提示词 | 文件 | 行号 | 用途 |
|---|---|---|---|
| 对话系统提示词 | `jetlag-backend/src/services/chatService.js` | 第 89-97 行 `buildSystemPrompt()` | AI 养生问答助手 |
| 图像特征提示词 | `jetlag-backend/src/qwenVision.js` | 第 172-185 行 | 舌/面/手图像特征提取 |

### 10.2 对话提示词当前内容与边界

```
你是"岐养七日"的中医养生问答助手，只能基于用户当前方案做日常养生解释和执行建议。
边界要求：不得诊断疾病，不得开药方，不得替代医生，不使用"确诊""治疗""处方"等医疗结论。
如果用户问到急症、严重疼痛、持续发热、出血、胸痛、呼吸困难、妊娠用药、儿童用药等高风险内容，必须建议线下就医或咨询执业医师。
回答要简洁、可执行，优先围绕饮食、运动、作息、观察记录四类建议，每次 3-5 条即可。
必须在回答末尾保留边界提醒：{DISCLAIMER}
当前方案摘要：{JSON.stringify(summarizeResult(result))}
```

### 10.3 图像特征提示词当前内容与边界

```
你是中医养生辅助系统的图像特征记录员，任务是记录舌像、面相、手相的可观察特征。
严格边界：只能描述图像特征和中医养生参考，不得输出疾病诊断，不得开处方，不得使用"确诊""治疗"等医疗结论。
请输出 JSON，不要 Markdown，不要额外解释。JSON 结构如下：
{ "tongue": {...}, "face": {...}, "palm": {...}, "overall_observation": [...], "caution": "..." }
如果某类图片没有提供，对应字段 features 为空数组，tcm_reference 写"未采集"。
规则引擎初步方向：{initialAnalysis.constitution.primary}
```

### 10.4 提示词限制建议（确保不影响其他模块）

以下为提示词安全限制建议，供开发者参考执行：

1. **输入边界限制**
   - 对话历史 `parsed.messages` 取最后 6 条（第 127 行 `slice(-6)`），防止超长上下文注入。建议维持。
   - `chatSchema` 的 `result` 字段使用 `z.object({}).passthrough()`（第 14 行），允许任意结构对象透传并拼入系统提示词。**存在提示词注入风险**：恶意构造的 result 对象可通过 `JSON.stringify` 注入额外指令。建议改为 `z.object({}).strict()` 或仅提取白名单字段。

2. **输出边界限制**
   - 对话 `max_tokens: 700`（第 135 行），图像 `max_tokens: 900`（第 191 行），限制输出长度。合理。
   - 对话 `temperature: 0.3`，图像 `temperature: 0.1`，降低随机性。合理。
   - `withDisclaimer()` 函数（第 100-104 行）确保每条回复末尾附带免责声明。合理。

3. **越界防护**
   - 两处提示词均已包含"不得诊断疾病、不得开处方"等边界声明。维持。
   - 对话提示词包含急症关键词触发就医建议的逻辑。维持。
   - 图像提示词要求输出纯 JSON 不含 Markdown，降低格式注入风险。维持。

4. **待加固项**
   - `result` 对象的 `passthrough()` 模式应改为白名单字段提取，防止提示词注入。
   - 用户提问 `parsed.question` 未经过滤直接拼入消息，建议增加长度上限（当前无限制）。
   - 图像提示词中的 `initialAnalysis.constitution.primary` 直接拼入，若该字段包含特殊字符可能干扰提示词结构，建议做转义或截断。
