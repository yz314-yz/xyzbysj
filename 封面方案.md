# 封面方案 - 岐养七日动态封面页

> 生成时间：2026-06-29
> 目标：为项目新增一个独立的动态封面页（landing），作为 `/` 路由入口，提升首屏视觉与答辩观感，再由封面进入 `/collection` 采集页。
> 技术栈约束：React 18 + Vite + react-router-dom 7 + lucide-react（与现有项目一致，不引入框架级冲突）

---

## 一、现状与目标

### 现状
- [App.jsx](jetlag-frontend/src/App.jsx#L52) 中 `/` 路由直接 `<Navigate to="/collection" replace />`，无独立封面。
- 首屏即进入采集表单，视觉单薄，缺少"品牌入口"。

### 目标
- 新增 `/` 封面页，含动态背景 + 品牌标题 + 进入按钮。
- 封面动态效果需契合中医养生主题（东方美学、气血流动、水墨意境）。
- 不破坏现有路由结构，`/collection`、`/history`、`/help`、`/about` 保持不变。
- 答辩演示无网络依赖（纯前端动画，不调用任何外部 API）。

---

## 二、候选方案对比

| # | 方案 | 仓库 | 效果风格 | 维护状态 | 包体积 | React 集成 | 中医契合度 | 集成难度 |
|---|------|------|----------|----------|--------|-----------|-----------|----------|
| 1 | **tsparticles** | [tsparticles/tsparticles](https://github.com/tsparticles/tsparticles) | 粒子流动/连线/星空 | 活跃（2026-05 仍在更） | 中（slim ~40KB gzip） | 官方 `@tsparticles/react` | 高（可模拟气血/经络流） | 低 |
| 2 | **Vanta.js** | [tengbao/vanta](https://github.com/tengbao/vanta) | 3D NETWAVES/RINGS/FOG/BIRDS | 已停更（2023-01） | 小（~15KB） | 需手动封装 ref | 中（FOG 像云雾） | 低 |
| 3 | **react-three-fiber** | [pmndrs/react-three-fiber](https://github.com/pmndrs/react-three-fiber) | 完全自定义 3D/着色器 | 活跃 | 大（three ~150KB） | 原生 React | 极高（可做太极/水墨球） | 高 |
| 4 | **anime.js + 自绘 SVG** | [juliangarnier/anime](https://github.com/juliangarnier/anime) | SVG 路径动画/笔触 | 活跃（v4） | 小（~17KB） | 需手动 | 高（可做经络线/水墨笔触） | 中 |
| 5 | **CSS 纯实现** | 无 | 渐变/呼吸/浮动 | - | 0 | 原生 | 中 | 极低 |
| 6 | **particles.js（老牌）** | [VincentGarreau/particles.js](https://github.com/VincentGarreau/particles.js) | 粒子连线 | 已停更 | 小 | 需封装 | 中 | 低 |

---

## 三、推荐方案：tsparticles（经络粒子流）

### 推荐理由
1. **契合主题**：粒子流动可隐喻"气血运行、经络流转"，与中医养生概念天然吻合。
2. **活跃维护**：v4 持续更新，React 官方适配包，TypeScript 支持。
3. **集成成本低**：`@tsparticles/react` + `@tsparticles/slim` 两个包，配置驱动，无需手写 shader。
4. **可控性强**：颜色、数量、速度、连线、鼠标交互均可配置，易调成青绿/墨色中医配色。
5. **无网络依赖**：纯 Canvas/WebGL 本地渲染，满足答辩硬约束。
6. **性能可控**：粒子数可调，移动端可降级。

### 备选触发条件
- 若想要"水墨晕染"质感 → 改用方案 4（anime.js + SVG 水墨笔触）或方案 3（r3f + 流体 shader）。
- 若想要极简零依赖 → 改用方案 5（CSS 渐变 + 呼吸动画）。

---

## 四、集成步骤（tsparticles）

### 4.1 安装依赖

```bash
cd jetlag-frontend
npm install @tsparticles/react @tsparticles/slim @tsparticles/engine
```

### 4.2 新增封面页组件

文件路径：`jetlag-frontend/src/pages/CoverPage.jsx`

```jsx
import { useEffect, useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Particles, { initParticlesEngine } from '@tsparticles/react';
import { loadSlim } from '@tsparticles/slim';
import { ArrowRight, Leaf } from 'lucide-react';

// 经络粒子流配置：青绿墨色，低速流动，鼠标轻推
function buildParticleOptions(theme) {
  const isDark = theme === 'dark';
  const linkColor = isDark ? '#3a7d5c' : '#7bbf9a';
  const particleColor = isDark ? '#9fd3b4' : '#2f6f54';
  return {
    fullScreen: { enable: false },
    background: { color: 'transparent' },
    fpsLimit: 60,
    detectRetina: true,
    particles: {
      color: { value: particleColor },
      links: {
        enable: true,
        color: linkColor,
        distance: 140,
        opacity: isDark ? 0.35 : 0.5,
        width: 1,
      },
      move: {
        enable: true,
        speed: 0.7,
        direction: 'none',
        outModes: { default: 'bounce' },
      },
      number: { value: 60, density: { enable: true, area: 900 } },
      opacity: { value: { min: 0.25, max: 0.7 } },
      shape: { type: 'circle' },
      size: { value: { min: 1, max: 3 } },
    },
    interactivity: {
      events: {
        onHover: { enable: true, mode: 'push' },
        onClick: { enable: true, mode: 'repulse' },
      },
      modes: {
        push: { quantity: 3 },
        repulse: { distance: 120 },
      },
    },
  };
}

export function CoverPage({ theme }) {
  const navigate = useNavigate();
  const [ready, setReady] = useState(false);
  const options = useMemo(() => buildParticleOptions(theme), [theme]);

  useEffect(() => {
    initParticlesEngine(async (engine) => {
      await loadSlim(engine);
    }).then(() => setReady(true));
  }, []);

  return (
    <section className="cover">
      {ready && <Particles id="tcm-cover" options={options} className="cover-canvas" />}
      <div className="cover-inner">
        <div className="cover-brand">
          <Leaf size={40} />
          <span className="cover-mark">岐</span>
        </div>
        <h1 className="cover-title">岐养七日</h1>
        <p className="cover-subtitle">中医养生辅助系统 · 望诊 · 子午流注 · 七日调理</p>
        <p className="cover-disclaimer">
          ⚠️ AI 分析仅供学术参考，不作为医疗诊断。请咨询执业中医师。
        </p>
        <button
          className="cover-enter"
          type="button"
          onClick={() => navigate('/collection')}
        >
          进入系统 <ArrowRight size={18} />
        </button>
      </div>
    </section>
  );
}
```

### 4.3 调整路由

修改 [App.jsx](jetlag-frontend/src/App.jsx#L51-L57)：

```jsx
// 新增 import
import { CoverPage } from './pages/CoverPage';

// Routes 内改为
<Routes>
  <Route path="/" element={<CoverPage theme={theme} />} />
  <Route path="/collection" element={<AssessmentPage auth={auth} {...runtimeData} />} />
  <Route path="/history" element={<HistoryPage auth={auth} />} />
  <Route path="/help" element={<HelpPage />} />
  <Route path="/about" element={<AboutPage />} />
</Routes>
```

> 注：原 `<Navigate to="/collection" replace />` 删除。侧边栏 `navItems` 中首页 `to="/"` 现在指向封面页，无需改动。

### 4.4 新增样式

在 [styles.css](jetlag-frontend/src/styles.css) 末尾追加：

```css
.cover {
  position: relative;
  min-height: calc(100vh - 2rem);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border-radius: 1rem;
}
.cover-canvas {
  position: absolute;
  inset: 0;
  z-index: 0;
}
.cover-inner {
  position: relative;
  z-index: 1;
  text-align: center;
  padding: 2rem;
  max-width: 640px;
}
.cover-brand {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  color: var(--accent, #2f6f54);
  margin-bottom: 1rem;
}
.cover-mark {
  font-size: 2rem;
  font-weight: 700;
}
.cover-title {
  font-size: clamp(2.5rem, 6vw, 4rem);
  margin: 0 0 0.5rem;
  letter-spacing: 0.25em;
}
.cover-subtitle {
  color: var(--muted, #5a6b62);
  margin: 0 0 1.5rem;
}
.cover-disclaimer {
  font-size: 0.8rem;
  color: var(--muted, #5a6b62);
  margin: 0 0 2rem;
  opacity: 0.85;
}
.cover-enter {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.75rem;
  font-size: 1rem;
  border-radius: 999px;
  border: 1px solid var(--accent, #2f6f54);
  background: var(--accent, #2f6f54);
  color: #fff;
  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.cover-enter:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(47, 111, 84, 0.35);
}
[data-theme="dark"] .cover-enter {
  background: #1f4a37;
  border-color: #3a7d5c;
}
```

---

## 五、中医主题视觉适配建议

### 配色（与现有 `:root` 变量对齐）
- 主色：青绿 `#2f6f54` / 墨青 `#1f4a37`
- 粒子：浅青绿 `#9fd3b4`（暗色）/ 深墨绿 `#2f6f54`（亮色）
- 连线：半透明青绿，营造"经络网"感

### 文案
- 主标题：**岐养七日**
- 副标题：中医养生辅助系统 · 望诊 · 子午流注 · 七日调理
- 免责声明：⚠️ AI 分析仅供学术参考，不作为医疗诊断。请咨询执业中医师。（与项目硬约束一致）
- 入口按钮：进入系统

### 进阶可选元素（不强制）
- 封面右下角放一个 CSS 旋转的太极图（`border-radius:50%` + 双色半圆 + `animation: spin 12s linear infinite`）。
- 标题下方加一行实时子午流注（调用现有 `/health` 已返回的 meridian，或前端按 `new Date().getHours()` 本地算）。

---

## 六、备选方案速览

### 备选 A：Vanta.js（云雾 FOG 效果）
- 仓库：[tengbao/vanta](https://github.com/tengbao/vanta)
- 优点：视觉冲击强，FOG 效果像水墨云雾。
- 缺点：**已停更（2023-01 最后提交）**，依赖旧版 three.js，与现有依赖可能冲突。
- 集成：`npm i vanta three`，在 `useEffect` 里 `VANTA.FOG({ el, THREE })`。

### 备选 B：react-three-fiber + 流体着色器
- 仓库：[pmndrs/react-three-fiber](https://github.com/pmndrs/react-three-fiber)
- 优点：可做太极球、水墨晕染、经络 3D 流动，效果天花板最高。
- 缺点：包体积大（three ~150KB），需写 GLSL 着色器，开发成本高。
- 适合：时间充裕、想冲"最佳设计奖"。

### 备选 C：anime.js + SVG 水墨笔触
- 仓库：[juliangarnier/anime](https://github.com/juliangarnier/anime)
- 思路：用 SVG 画几条经络线，anime.js 做"笔触绘制"动画（`strokeDashoffset`）。
- 优点：体积小，纯 SVG 清晰可缩放，最贴"水墨"意境。
- 缺点：需自己画 SVG 路径。

### 备选 D：纯 CSS（零依赖兜底）
- 思路：多层径向渐变 + `@keyframes` 呼吸缩放 + 浮动光斑。
- 优点：零依赖，性能最佳。
- 缺点：效果朴素。

---

## 七、风险与注意事项

1. **粒子性能**：低端机 60 粒子可能掉帧，建议 `number.value` 在移动端降到 30（可用 `window.innerWidth < 768` 判断）。
2. **首屏白屏**：`initParticlesEngine` 异步加载期间封面会短暂无粒子，已用 `ready` state 控制，期间只显示标题文字（可接受）。
3. **暗色主题联动**：封面需读取 `theme` 状态切换粒子配色，方案四代码已处理（`theme` 由 [App.jsx](jetlag-frontend/src/App.jsx#L22) 传入）。
4. **答辩演示**：tsparticles 纯本地渲染，无网络请求，满足"答辩不得用云端"硬约束。
5. **License**：tsparticles 为 MIT，可自由用于毕业设计。
6. **bundle 体积**：`@tsparticles/slim` gzip 后约 40KB，可接受；若极致追求体积可用 `@tsparticles/basic`（更小，去掉部分预设）。

---

## 八、实施检查清单

- [ ] 安装 `@tsparticles/react @tsparticles/slim @tsparticles/engine`
- [ ] 新建 `src/pages/CoverPage.jsx`
- [ ] 修改 `App.jsx` 路由：`/` → CoverPage
- [ ] `styles.css` 追加 `.cover*` 样式
- [ ] 验证暗色主题下粒子配色正常
- [ ] 验证移动端粒子降级（可选）
- [ ] 验证"进入系统"按钮跳转 `/collection`
- [ ] 确认免责声明文案含 ⚠️ 前缀
- [ ] `npm run build` 通过
- [ ] 前端测试 `npm test` 通过（必要时 mock Particles）

---

## 九、参考仓库

| 仓库 | 用途 |
|------|------|
| [tsparticles/tsparticles](https://github.com/tsparticles/tsparticles) | 推荐方案主库（粒子引擎） |
| [pmndrs/react-three-fiber](https://github.com/pmndrs/react-three-fiber) | 备选 B（3D 着色器） |
| [juliangarnier/anime](https://github.com/juliangarnier/anime) | 备选 C（SVG 笔触动画） |
| [tengbao/vanta](https://github.com/tengbao/vanta) | 备选 A（3D 背景预设，已停更） |
| [VincentGarreau/particles.js](https://github.com/VincentGarreau/particles.js) | 老牌粒子库参考（不推荐新项目用） |
