# Reference template only.
# Adapt this file based on the actual project structure, package manager, build output, and deployment requirements.
# Do not use blindly without checking the project files.

node_modules
dist
build
.git
.env
.env.*
npm-debug.log
yarn-debug.log
yarn-error.log

# Be careful with lock files.
# Do not ignore package-lock.json, pnpm-lock.yaml, yarn.lock, or bun.lockb unless you confirm they are not needed for reproducible installs.
