# 待改进清单 - 岐养七日 · 中医养生辅助系统

> 生成时间：2026-06-29（基于全量代码扫描重写）
> 扫描范围：前端 (jetlag-frontend/src/)、后端 (jetlag-backend/src/)、部署配置、Docker、CI/CD、毕业设计学术规范
> 说明：本文档已根据当前真实代码状态重写，旧版（2026-06-28）所述"无数据库/无用户体系/无测试/无学术文档"等结论均已不成立，请勿参考旧版。

---

## 总体评价

项目工程化水平已达到**优秀**标准，远超本科毕业设计平均水平。前版清单中 60 项问题绝大多数已修复，当前主要短板集中在**功能闭环细节、答辩演示路径、文档一致性**三个维度。

### 已完成的关键改进（旧版 P0 全部解决）

| 维度 | 旧版状态 | 当前状态 |
|------|----------|----------|
| 学术文档 | 全部缺失 | ✅ docs/ 下齐全（需求/设计/数据库/测试/手册） |
| 数据库 | 无持久化 | ✅ SQLite + better-sqlite3，users/diagnoses 两表 |
| 用户认证 | 无 | ✅ JWT + bcrypt，register/login/me 完整 |
| 测试 | 仅语法检查 | ✅ Jest+Supertest（后端）、Vitest+Testing Library（前端） |
| 后端分层 | 单文件 550 行 | ✅ controller/service/model/routes/middleware 五层 |
| 前端结构 | 单文件 639 行 | ✅ components/pages/hooks/services/constants 拆分 |
| 路由 | 单页滚动 | ✅ react-router-dom 7，4 个路由页 |
| API 文档 | 无 | ✅ swagger-jsdoc + swagger-ui-express，/api-docs |
| 日志 | console | ✅ winston + daily-rotate-file，HTTP/应用/错误三分流 |
| 安全头 | 无 | ✅ helmet + CSP，frame-ancestors none |
| 限流 | 无 | ✅ express-rate-limit，diagnose 接口独立窗口 |
| 输入校验 | 手写 | ✅ zod schema |
| MIME 校验 | 无 | ✅ 白名单 jpeg/png/webp |
| 错误中间件 | 无 | ✅ 统一 errorHandler，脱敏 + 分级日志 |
| 容器安全 | root 运行 | ✅ 三个 Dockerfile 均 USER node + HEALTHCHECK |
| CI 质量 | 无测试 | ✅ lint + test + build 三步，多 tag + gha 缓存 |
| 主题切换 | 无 | ✅ 明暗主题 + localStorage 持久化 |
| ErrorBoundary | 无 | ✅ 全局 ErrorBoundary |
| 监控 | 无 | ✅ Prometheus /metrics + 可选 Grafana/Loki 栈 |

---

## 一、P0 — 答辩前必处理

### 1. 免责声明文案与项目硬约束不一致

- **文件**：[jetlag-backend/src/services/analysisService.js](jetlag-backend/src/services/analysisService.js#L222)、[jetlag-frontend/src/constants/app.js](jetlag-frontend/src/constants/app.js#L46)
- **问题**：项目硬约束规定所有结果页必须展示固定文案：`⚠️ AI 分析仅供学术参考，不作为医疗诊断。请咨询执业中医师。`
- **现状**：实际文案为 `AI 分析仅供学术展示与日常养生参考，不能替代执业医师诊断、治疗或处方。`
  - 缺少 `⚠️` 前缀
  - 措辞与约束规定不同
- **建议**：统一为约束规定文案，前后端两处同步修改。

### 2. 答辩演示路径需确认走纯本地规则引擎

- **文件**：[.env.example](.env.example)、[docs/用户手册.md](docs/用户手册.md)
- **问题**：项目硬约束"答辩期间不得使用云端大模型（防网络问题）"。当前 `OPEN_MODEL_BASE_URL=` 默认为空时本地规则引擎可独立工作（安全），但 `.env.example` 和文档仍引导配置硅基流动云端 API。
- **建议**：
  - 答辩前确认演示环境 `OPEN_MODEL_BASE_URL` 留空
  - 在用户手册中明确标注"答辩演示模式 = 本地规则引擎（无网络依赖）"
  - 可在 README 增加一段"演示模式"说明

### 3. 旧版 `待改进.md` 已被本文件取代

- **说明**：本文件即为此项的解决方案。旧版描述的"四无"已全部不成立，答辩老师若误读旧版会形成负面预设。

---

## 二、P1 — 功能完整性缺口

### 4. 历史详情接口已实现但前端未接入

- **文件**：[jetlag-backend/src/routes/diagnosisRoutes.js](jetlag-backend/src/routes/diagnosisRoutes.js#L54)、[jetlag-frontend/src/pages/HistoryPage.jsx](jetlag-frontend/src/pages/HistoryPage.jsx)
- **问题**：后端有 `GET /api/v1/history/:id`，但前端历史页只展示摘要列表，点击无响应，无法查看完整报告。接口白写。
- **建议**：历史条目改为可点击，展开/跳转展示完整七日计划与图像特征。

### 5. 子午流注定时提醒无实际投递

- **文件**：[jetlag-backend/src/services/reminderService.js](jetlag-backend/src/services/reminderService.js#L11)
- **问题**：cron 任务仅 `logger.info`，无邮件/WebSocket/站内信任何投递。README 宣称"时辰养生提醒"功能，实际是空壳。
- **建议**：二选一
  - 补一个 SSE 或 WebSocket 推送到前端通知组件
  - 在文档中诚实说明"目前仅服务端日志记录，作为定时任务框架演示"

### 6. 前端无图片压缩，与工程约定不符

- **文件**：[jetlag-frontend/src/components/FileDrop.jsx](jetlag-frontend/src/components/FileDrop.jsx)
- **问题**：项目约定"图片压缩：1080P、JPEG、0.8 质量"。当前 FileDrop 只做 MIME 与 8MB 校验，上传前无任何压缩。
- **建议**：选图后用 `canvas` 缩放至 1080P + `toBlob('image/jpeg', 0.8)` 压缩再上传，节省带宽并加速 Qwen 推理。

### 7. 无光线不足提示

- **文件**：[jetlag-frontend/src/components/FileDrop.jsx](jetlag-frontend/src/components/FileDrop.jsx)
- **问题**：项目约定"未开补光灯需提示'请在光线充足处拍摄'"。当前只有软提示文案"自然光、伸舌平拍"，无实际检测或强提示。
- **建议**：选图后用 canvas 读取平均亮度，低于阈值则 toast 警告"请在光线充足处拍摄"。

---

## 三、P2 — 工程质量

### 8. 测试覆盖偏薄

- **文件**：[jetlag-backend/test/api.test.js](jetlag-backend/test/api.test.js)、[jetlag-frontend/src/App.test.jsx](jetlag-frontend/src/App.test.jsx)
- **问题**：
  - 后端仅 3 个 test，未覆盖：登录失败、错误密码、未授权访问历史、图片上传、限流触发、JWT 过期/伪造
  - 前端仅 2 个 test，未覆盖：登录注册流程、历史页、PDF 导出、深色主题、错误状态、文件上传交互
- **建议**：答辩前至少补"登录失败 + 未授权访问 + 图片上传"三个后端用例，"登录流程 + 历史页空态"两个前端用例。

### 9. JWT 默认密钥无生产环境告警

- **文件**：[jetlag-backend/src/config.js](jetlag-backend/src/config.js#L17)、[jetlag-backend/src/server.js](jetlag-backend/src/server.js)
- **问题**：`jwtSecret: process.env.JWT_SECRET || 'dev-only-change-me'`，生产环境漏配会静默使用弱密钥。docker-compose.cloud.yml 已用 `${JWT_SECRET:?...}` 强制，但根 Dockerfile 和独立后端 Dockerfile 没有。
- **建议**：在 server.js 启动时检测 `NODE_ENV=production && !process.env.JWT_SECRET` 则 `logger.error` 并 `process.exit(1)`。

### 10. docker-compose.cloud.yml 中 VITE_API_BASE 是死配置

- **文件**：[docker-compose.cloud.yml](docker-compose.cloud.yml#L39)
- **问题**：给 frontend 容器设 `VITE_API_BASE` 运行期 env，但 Vite 变量是构建期注入，对已构建的静态文件无效。实际运行靠 `/env.js` 注入 `window.__APP_CONFIG__`（已正确实现）。
- **建议**：删除该 env 行，或加注释说明"此值无效，仅保留兼容"。

### 11. HistoryPage 无加载态

- **文件**：[jetlag-frontend/src/pages/HistoryPage.jsx](jetlag-frontend/src/pages/HistoryPage.jsx)
- **问题**：请求历史时无 loading 指示，用户看到空列表会误以为没数据。
- **建议**：增加 `loading` state，请求中显示"加载中…"占位。

### 12. AuthPanel 表单交互不一致

- **文件**：[jetlag-frontend/src/components/AuthPanel.jsx](jetlag-frontend/src/components/AuthPanel.jsx#L33)
- **问题**：form 有 `onSubmit`，但提交按钮是 `type="button" onClick={submit}`。输入框按 Enter 走 onSubmit，按钮走 onClick，逻辑分叉。同时输入框缺 `autoComplete`。
- **建议**：按钮改 `type="submit"` 统一；输入框补 `autoComplete="username"` / `"current-password"`。

### 13. metrics.js 声明 summary 类型但无分位数

- **文件**：[jetlag-backend/src/metrics.js](jetlag-backend/src/metrics.js#L95)
- **问题**：声明 `# TYPE tcm_http_request_duration_seconds summary`，只输出 `_sum` 和 `_count`，无 quantile。Prometheus 不会报错但不完整。
- **建议**：补 quantile 分位数，或改声明为自定义 counter。

---

## 四、P3 — 锦上添花

### 14. 知识库硬编码未入库

- **文件**：[jetlag-backend/src/tcmKnowledge.js](jetlag-backend/src/tcmKnowledge.js)
- **说明**：症状、体质、子午流注、七日主题全部硬编码。答辩可解释为"领域知识常量"，可接受。如需更"工程化"，可迁移到 SQLite 表。

### 15. 历史详情 id 未校验 NaN

- **文件**：[jetlag-backend/src/services/historyService.js](jetlag-backend/src/services/historyService.js#L9)
- **问题**：`Number(id)` 未校验 NaN，传非数字 id 会查空返回 404（行为 OK 但不够健壮）。
- **建议**：加 `Number.isFinite` 校验，提前返回 400。

### 16. 评分公式缺少依据说明

- **文件**：[jetlag-backend/src/services/analysisService.js](jetlag-backend/src/services/analysisService.js#L231)
- **问题**：`confidence: Math.min(95, 58 + primary.score * 8 + ...)` 是经验值，文档未说明依据。
- **建议**：在 [docs/系统设计.md](docs/系统设计.md) 补一段"置信度算法说明"。

### 17. 根 Dockerfile 镜像偏大

- **文件**：[Dockerfile](Dockerfile)
- **问题**：装了 `python3 make g++` 编译 better-sqlite3 后未清理，镜像偏大（可接受）。
- **建议**：可用多阶段构建把编译工具留在构建层，运行层不含。优先级最低。

### 18. 无封面/欢迎页

- **文件**：[jetlag-frontend/src/App.jsx](jetlag-frontend/src/App.jsx#L52)
- **问题**：`/` 路由直接 redirect 到 `/collection`，无独立封面页，首屏体验单薄。
- **建议**：见独立文档 `封面方案.md`，引入开源动态封面作为 landing。

---

## 五、改进顺序建议

```
第1步：P0 - 答辩必补
  ├─ 统一免责声明文案（⚠️ 前缀 + 固定措辞）
  ├─ 确认答辩演示走本地规则引擎 + 文档标注
  └─ （本文件已取代旧版待改进.md）
       ↓
第2步：P1 - 功能闭环
  ├─ 历史详情前端入口（接 /api/v1/history/:id）
  ├─ 定时提醒投递或文档诚实说明
  ├─ 前端图片压缩（canvas + JPEG 0.8）
  ├─ 光线不足提示
  └─ 引入动态封面页（见 封面方案.md）
       ↓
第3步：P2 - 工程质量
  ├─ 补测试用例（登录失败/未授权/上传）
  ├─ JWT 生产密钥告警
  ├─ 清理 docker-compose 死配置
  ├─ HistoryPage 加载态
  ├─ AuthPanel 表单统一
  └─ metrics summary 分位数
       ↓
第4步：P3 - 锦上添花
  ├─ 知识库入库（可选）
  ├─ 置信度算法文档
  └─ 镜像多阶段构建
```

---

## 六、问题统计

| 优先级 | 数量 | 说明 |
|--------|------|------|
| P0（答辩必补） | 3 项 | 免责声明、演示路径、旧文档清理 |
| P1（功能闭环） | 5 项 | 历史详情、提醒投递、图片压缩、光线提示、封面页 |
| P2（工程质量） | 6 项 | 测试、JWT、死配置、加载态、表单、metrics |
| P3（锦上添花） | 5 项 | 知识库、id 校验、算法文档、镜像、封面 |
| **总计** | **19 项** | （旧版 60 项，已修复 41 项） |

---

## 七、一句话总结

> **工程实现已属优秀，P0 答辩硬伤仅剩"免责声明文案对齐 + 演示路径确认"两项，处理完即无硬伤；P1 主要是历史详情闭环和封面页两件体验事；P2/P3 均为锦上添花，不影响答辩通过。**
