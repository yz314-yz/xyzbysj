# 岐养七日 - 待改进清单

> 生成时间：2026-07-03
> 基于全量代码审查和交互测试

---

## 一、需要你决策的难题（我改起来有风险，建议你自己定）

这些是涉及架构权衡或第三方库限制的问题，我无法单方面确定最优方案：

### 1. 移动端触摸翻页与桌面误触的根本冲突

**现状**：为解决"点症状按钮误触发翻页"，我设置了 `useMouseEvents={false}`，彻底禁用了 react-pageflip 的所有鼠标和触摸事件。

**问题**：
- 桌面端：鼠标拖拽翻页被禁用（但有左右箭头按钮和键盘，影响不大）
- 移动端：**触摸滑动翻页完全失效**，手机用户只能点底部箭头按钮翻页
- `react-pageflip` 库的 `useMouseEvents` 是 mouse+touch 的总开关，没有单独禁用 mouse 保留 touch 的选项
- 代码中的 `mobileEventSupport` 和 `showMobileView` 两个 props 实际上是**无效的**（库源码中不存在这两个配置项），不会报错但也不起作用

**你需要决定**：
- 方案A：保持现状（`useMouseEvents={false}`），移动端只能按钮翻页，但症状/表单交互完全正常
- 方案B：改回 `useMouseEvents={true}` + `disableFlipByClick`，桌面端点击不翻页但触摸滑动恢复，但可能在某些操作时仍有误触
- 方案C：自己 fork react-pageflip 库，分离 mouse 和 touch 事件绑定逻辑
- 方案D：换一个翻页库（如 turn.js 或自研）

### 2. 翻页库的无效 props

`BookExperience.jsx` 第 257-258 行：
```jsx
mobileEventSupport    // 无效，库源码无此配置
showMobileView        // 无效，库源码无此配置
```
这两个 props 被 `Object.assign` 合并进 settings 但没有任何代码读取。删除不会影响功能，但留着会误导维护者以为移动端有特殊支持。

**建议**：删除这两个无效 props，避免误解。

### 3. tocItems 硬编码页码依赖七日计划固定7页

`BookExperience.jsx` 第 226-235 行，目录的页码是硬编码的：
```js
{ page: 4, title: '七日调理' },
{ page: 11, title: '时辰养生' },  // 假设七日计划正好7页(4-10)
{ page: 12, title: '诊史' },
{ page: 13, title: '跋' },
```
如果后端返回的 `sevenDayPlan` 长度 ≠ 7，时辰/诊史/跋的页码会全部偏移，目录跳转会跳到错误页面。

**你需要决定**：是否根据实际 `result.sevenDayPlan.length` 动态计算页码，还是接受固定7页的假设。

### 4. 大量孤立代码增加维护成本

翻书改造前的旧布局代码仍然保留在项目中：

| 类型 | 文件 |
|---|---|
| 孤立组件 | `Sidebar.jsx`、`SymptomsPanel.jsx`、`MeridianActions.jsx`、`PlanTable.jsx`、`CheckinPanel.jsx`（独立版） |
| 孤立页面 | `AboutPage.jsx`、`AssessmentPage.jsx`、`CoverPage.jsx`、`HelpPage.jsx`、`HistoryPage.jsx` |
| 孤立Hooks | `useSectionObserver.js` |
| 孤立脚本 | `particlesInit.js` |
| 孤立CSS | `.shell`、`.sidebar`、`.workspace`、`.topbar`、`.cover` 等大量旧样式 |

styles.css 已达 66KB，其中相当部分是旧布局样式。`App.test.jsx` 还引用部分孤立组件。

**你需要决定**：是否清理这些孤立代码，还是保留备用。

### 5. CheckinPanel 双重实现

`BookExperience.jsx` 内联了一套简化版打卡 UI（第400-455行），同时存在独立的 `CheckinPanel.jsx`（功能更完善，含摘要统计、天数选择器）。两套实现行为略有差异：
- 内联版保存按钮始终显示"保存打卡"，不显示加载文案
- 独立版保存时显示"保存中..."
- 评分按钮在内联版无 `aria-label`，独立版有

**你需要决定**：统一用哪一套，还是重构合并。

---

## 二、明确Bug（我可以修，但需要你确认优先级）

### P0 - 页码显示错误

**文件**：`BookExperience.jsx` 第 471/493/536 行

最后三页的 `pageNum` prop 传错：
| 页面 | 当前 pageNum | 显示页码 | 应显示 |
|---|---|---|---|
| 时辰养生 | `12` | 十二 | 十一 |
| 诊史 | `13` | 十三 | 十二 |
| 跋 | `14` | 十四 | 十三 |

页码从"十"直接跳到"十二"，跳过了"十一"。

**修复**：改为 `pageNum={11}`、`pageNum={12}`、`pageNum={13}`

### P0 - LoginPage 返回链接指向不存在路由

**文件**：`LoginPage.jsx` 第 23 行
```jsx
<Link to="/collection">返回望诊采集</Link>
```
App.jsx 无 `/collection` 路由，点击会被 `*` 重定向到首页。

**修复**：改为 `to="/book"`

### P1 - 历史详情加载失败后状态不一致

**文件**：`BookExperience.jsx` 第 211-219 行

`toggleHistory()` 在 `loadHistoryDetail` 失败时，仅 toast 报错但不重置 `expandedHistoryId`。导致卡片保持展开状态（有 `is-open` 样式），但详情区域为空。用户看到"空白展开卡片"。

对比：独立 `HistoryPage.jsx` 在失败时会 `setExpandedId(null)` 收起卡片。

**修复**：失败时 `setExpandedHistoryId(null)`

### P1 - 历史详情可能显示 undefined

**文件**：`BookExperience.jsx` 第 520 行
```jsx
{historyDetail[item.id].result?.constitution?.primary}
```
如果 `constitution` 为 null，会显示"主方向：undefined"而非友好提示。

**修复**：加空值判断 `{... || '暂无'}`

---

## 三、交互问题（体验不好，可以优化）

### 1. 首页无主题切换入口

**文件**：`LandingHero.jsx` / `App.jsx`

App.jsx 将 `theme` 传给了 LandingHero，但 LandingHero 内部未渲染 ThemeToggle 组件。用户在首页无法切换明暗模式。

**修复**：在首页右上角加 ThemeToggle 组件。

### 2. TOC打开时键盘方向键仍翻页

**文件**：`BookExperience.jsx` 第 127-136 行

键盘监听在 `window` 上，TOC 弹窗（z-index:20）打开时按方向键会在弹窗背后翻页，用户无感知但页面已变化。

**修复**：`showToc` 为 true 时跳过键盘处理：
```js
if (showToc) return;
```

### 3. TOC缺少ESC关闭和焦点管理

弹窗打开后按 Escape 不会关闭，焦点也未移入面板。

**修复**：添加 keydown 监听 ESC + focus 管理。

### 4. ChatPanel新消息不自动滚动到底部

**文件**：`ChatPanel.jsx`

新消息添加时 `chat-thread`（max-height:360px）不会自动滚到底部，用户需手动下滑查看 AI 回复。

**修复**：useEffect 监听消息变化，`scrollTo(0, scrollHeight)`

### 5. 所有日页面保存按钮同时显示加载状态

**文件**：`BookExperience.jsx` 第 449 行

`checkinSaving` 是单一布尔值，保存某天打卡时，所有7个日页面的保存按钮都显示 spinner 并禁用。

**修复**：改为 `checkinSavingDay` 存储当前保存的天数。

### 6. 时辰提示不实时刷新

**文件**：`LandingHero.jsx`、`BookExperience.jsx`

`meridianHint` 用 `useMemo(() => getMeridian(new Date().getHours()), [])` 只在组件挂载时计算一次。用户长时间停留跨时辰时，提示不会更新。

**修复**：增加定时器每分钟检查一次。

### 7. InkBackground主题切换闪烁

**文件**：`InkBackground.jsx`

`useEffect` 依赖 `[theme]`，切换主题时整个 canvas 动画重新初始化，可能产生短暂闪烁。

**修复**：分离 canvas 初始化和主题渲染，或用 CSS 变量替代 JS 切换。

### 8. 表单无字段级验证

**文件**：`BookExperience.jsx` 问诊页

年龄、性别等字段为空时不阻止提交，仅验证是否有症状或图片。

**修复**：提交前校验必填字段。

---

## 四、代码质量问题

### 1. 死代码

| 文件 | 问题 |
|---|---|
| `styles.css` 第 354-364 行 | `.theme-toggle-dot` 样式不再使用（ThemeToggle 已改用 lucide 图标） |
| `BookExperience.jsx` 第 247-250 行 | `size="fixed"` 下 `minWidth/maxWidth/minHeight/maxHeight` 四个 props 无效（库会覆盖为 width/height） |
| `ThemeToggle.jsx` | `.theme-toggle` 类名无任何样式定义，全靠 `.icon-action` 提供样式 |

### 2. 重复定义

- `MERIDIAN_TABLE` 在 `LandingHero.jsx` 和 `BookExperience.jsx` 各定义了一份（后者多了 `range` 字段），应提取到 `constants/app.js`
- 两套主题切换按钮样式不统一：`ThemeToggle` 用 `.icon-action`，`BookExperience` 用 `.book-theme-btn`

### 3. 性能小问题

- `InkBackground.jsx`：`reduceMotion=true` 时 canvas 的 `requestAnimationFrame` 仍持续循环（每50ms空 `clearRect`），应直接不启动循环
- 图片压缩为同步阻塞主线程，大图片会卡顿，可考虑 Web Worker

### 4. 可访问性

- 七日调理页评分按钮无 `aria-label`（第431行）
- TOC 弹窗无 `role="dialog"` 和 `aria-modal`

---

## 五、移动端问题

### 1. 极窄屏幕尺寸冲突

`useBookDimensions` 在 `vw < 768` 时设置 `width: Math.min(vw - 24, 440)`。对于 320px 宽手机，width=296px，但 `minWidth={320}` 可能导致库强制最小宽度 320px，产生布局溢出。

### 2. 问诊页表单拥挤

在 296px 宽的书籍内，`.profile-row` 的 2列布局每列仅约 120px，输入框非常拥挤。

### 3. 目录面板偏窄

`.book-toc-panel` 的 `max-width:80vw` 在小屏幕上可能过窄，列表项文字可能被截断。

---

## 六、快速修复清单（我可以立即执行的）

如果你确认，以下问题我可以马上修：

- [ ] pageNum 页码 11/12/13（改3个数字）
- [ ] LoginPage 返回链接改为 `/book`
- [ ] 历史详情失败时收起卡片
- [ ] 历史详情 undefined 显示"暂无"
- [ ] TOC打开时禁用键盘翻页
- [ ] TOC添加ESC关闭
- [ ] ChatPanel自动滚动到底部
- [ ] 保存按钮改为按天显示加载状态
- [ ] 删除无效的 mobileEventSupport/showMobileView props
- [ ] 删除死代码 .theme-toggle-dot
- [ ] 评分按钮加 aria-label

需要你决策的难题（第一部分）请告诉我你的选择，我再执行。

---

## 七、后端安全与健壮性问题（第二轮深度审查）

> 以下为对 `jetlag-backend` 和前端API层的深度审查发现，按严重程度分类。

### 严重问题（Critical）

#### 1. API密钥明文存储在本地环境文件
- **文件**：`.env.cloud` 第30行
- **内容**：`OPEN_MODEL_API_KEY=<已吊销>`（阿里云百炼/DashScope真实密钥）
- **说明**：虽被 `.gitignore` 忽略，但项目记忆中已记录"暴露API密钥存在安全风险，必须重新生成"。该密钥应立即吊销并重新生成。

#### 2. 生产环境JWT密钥明文存储
- **文件**：`jetlag-backend/.env` 第7行
- **内容**：`JWT_SECRET=hf-prod-9f3a7c2e...`，且 `NODE_ENV=production`
- **说明**：明文存储在本地文件中，如文件被意外共享或机器被入侵，攻击者可伪造任意用户令牌。

#### 3. JWT令牌存储在localStorage（XSS可窃取）
- **文件**：`jetlag-frontend/src/hooks/useAuth.js` 第6、9、41、51行
- **说明**：localStorage可通过JavaScript访问，一旦发生XSS漏洞，攻击者可直接窃取令牌。更安全的方案是使用 httpOnly Cookie。

#### 4. 认证端点缺少速率限制（暴力破解风险）
- **文件**：`jetlag-backend/src/routes/authRoutes.js` 第15、24行
- **说明**：`/api/v1/auth/register` 和 `/api/v1/auth/login` 无任何速率限制。攻击者可暴力破解密码或批量注册。对比：`/api/v1/diagnose` 已配置限速（10次/分钟）。

### 中等问题（Medium）

#### 5. 诊断和对话端点允许匿名访问（成本/资源滥用）
- **文件**：`jetlag-backend/src/routes/diagnosisRoutes.js` 第29行，`chatRoutes.js` 第15行
- **说明**：使用 `optionalAuth` 而非 `requireAuth`。匿名用户可上传图片触发Qwen Vision API调用（产生费用），也可使用AI对话。

#### 6. SSE端点无认证（资源消耗）
- **文件**：`jetlag-backend/src/routes/meridianRoutes.js` 第14行
- **说明**：`/api/v1/meridian-stream` 无认证要求，每个连接创建30秒心跳定时器，未认证的批量连接可消耗服务器资源。

#### 7. 健康检查和指标端点暴露内部信息
- **文件**：`jetlag-backend/src/routes/healthRoutes.js` 第14行，`metricsRoutes.js` 第14行
- **说明**：`/health` 公开返回视觉模型配置（baseURL、模型名、provider状态），`/metrics` 暴露Prometheus指标。生产环境应限制访问。

#### 8. 请求体大小限制过大（DoS风险）
- **文件**：`jetlag-backend/src/app.js` 第102-103行
- **说明**：`express.json({ limit: '20mb' })`。JSON载荷20MB过大，诊断接口通过multipart上传图片（有8MB限制），JSON接口不需要这么大。建议降至1MB。

#### 9. 文件上传仅校验MIME类型头（可绕过）
- **文件**：`jetlag-backend/src/middleware/upload.js` 第15-22行
- **说明**：`fileFilter` 仅检查 `file.mimetype`（客户端发送的Content-Type头），未校验文件实际内容（magic bytes/文件签名）。攻击者可伪造MIME类型。

#### 10. 环境变量类型转换缺少校验
- **文件**：`jetlag-backend/src/config.js` 第27-28行
- **说明**：`Number(process.env.DIAGNOSE_RATE_MAX || 10)` 如果设为非数字字符串，结果为 `NaN`，速率限制可能失效。应添加 `Number.isFinite()` 校验。

#### 11. 默认JWT密钥弱（开发环境）
- **文件**：`jetlag-backend/src/config.js` 第17行
- **说明**：`jwtSecret: process.env.JWT_SECRET || 'dev-only-change-me'`。`server.js` 有生产环境检查，但若未设置 `NODE_ENV=production` 而应用又对外可访问，弱密钥会被使用。

#### 12. bcrypt加密轮数偏低
- **文件**：`jetlag-backend/src/services/authService.js` 第35行
- **说明**：`bcrypt.hash(payload.password, 10)` 使用10轮，是最低推荐值。生产环境建议12轮以上。

#### 13. 数据库无加密存储
- **文件**：`jetlag-backend/data/tcm-wellness.sqlite`
- **说明**：SQLite明文文件存储，包含用户密码哈希、体质诊断结果、个人健康信息（年龄、性别、作息）。无静态加密。

#### 14. Chat请求体result字段允许任意对象透传（提示词注入风险）
- **文件**：`jetlag-backend/src/services/chatService.js` 第14行
- **说明**：`result: z.object({}).passthrough()` 允许任意结构对象通过，经 `JSON.stringify` 拼入AI系统提示词。恶意构造的对象可注入额外指令。建议改为白名单字段提取。

### 轻微问题（Minor）

#### 15. SSE连接无重连机制
- **文件**：`jetlag-frontend/src/hooks/useMeridianStream.js` 第40-43行
- **说明**：`source.onerror` 直接关闭连接且不重试。网络抖动后用户无法收到时辰养生提醒，需刷新页面恢复。

#### 16. 历史记录加载潜在竞态条件
- **文件**：`jetlag-frontend/src/components/BookExperience.jsx` 第89-94行
- **说明**：`loadHistory` 的useEffect没有使用cancelled标志（对比 `useAuth.js` 第14行有正确使用）。token快速变化时旧请求可能覆盖新数据。

#### 17. 无Token刷新机制
- **文件**：`jetlag-frontend/src/hooks/useAuth.js`
- **说明**：JWT有效期7天，无刷新令牌机制。令牌过期后用户需重新登录。

#### 18. EventSource无法发送认证头
- **文件**：`jetlag-frontend/src/hooks/useMeridianStream.js` 第16行
- **说明**：EventSource API不支持自定义Header，当前SSE端点也无认证。未来如需对SSE加认证，需改用fetch+ReadableStream。

#### 19. nginx无HTTPS配置
- **文件**：`jetlag-frontend/nginx.conf` 第2行
- **说明**：仅监听8080端口（HTTP），无TLS配置。生产环境应在反向代理层启用HTTPS并设置HSTS。

#### 20. CSP允许unsafe-inline
- **文件**：`jetlag-backend/src/app.js` 第72-73行
- **说明**：`script-src: ["'self'", "'unsafe-inline'"]`，降低CSP对XSS的防护效果。考虑使用nonce或hash替代。

#### 21. 诊断结果个人健康信息持久化无保留策略
- **文件**：`jetlag-backend/src/models/diagnosisModel.js` 第21-32行
- **说明**：诊断结果以JSON明文存入SQLite，无数据保留策略或自动清理机制。`listDiagnosesByUser` 限制返回50条，但数据库中无总量限制。

#### 22. 错误日志记录完整堆栈
- **文件**：`jetlag-backend/src/middleware/errors.js` 第48行
- **说明**：`logger.error('全局错误处理', { error: error.stack })` 将完整堆栈写入日志文件，可能包含敏感路径信息。

### 测试覆盖不足

#### 23. 前端测试覆盖率低
- **文件**：`jetlag-frontend/src/App.test.jsx`（仅4个测试）
- **说明**：未覆盖API调用错误处理、认证流程、诊断提交流程、打卡功能、SSE连接、PDF导出等核心功能。

#### 24. 后端测试未覆盖的安全场景
- **文件**：`jetlag-backend/test/api.test.js`（17个测试）
- **说明**：未测试CORS来源拒绝、无效MIME文件上传、速率限制触发、非拥有者访问他人诊断记录、并发打卡写入、超大请求体拒绝等。

### 已正确实施的安全措施（确认无需修改）

| 安全措施 | 文件位置 |
|---|---|
| 密码bcrypt哈希存储 | `authService.js:35` |
| JWT带过期时间 | `authService.js:15-19` |
| 生产环境强制JWT密钥检查 | `server.js:8-14` |
| Zod schema输入校验 | `authService.js`、`analysisService.js`、`checkinService.js`、`chatService.js` |
| 参数化SQL查询（防注入） | `userModel.js`、`diagnosisModel.js`、`checkinModel.js` |
| Helmet安全头 | `app.js:63-78` |
| 文件上传大小限制（8MB） | `upload.js:14` |
| 文件类型白名单 | `upload.js:8,15-22` |
| 诊断端点速率限制 | `diagnosisRoutes.js:11-17` |
| 上传临时文件清理 | `diagnosisController.js:16`、`upload.js:24-32` |
| CORS来源白名单 | `app.js:35-57` |
| 用户数据所有权校验 | `checkinService.js:19-24`、`historyService.js:9-21` |
| 无dangerouslySetInnerHTML | 前端全局确认 |
| React自动转义防XSS | 所有组件使用JSX渲染 |
| 错误处理不暴露堆栈给客户端 | `errors.js:43-44,53` |
| SQL外键约束启用 | `database.js:15` |
| Docker使用非root用户 | `Dockerfile` |
| ObjectURL正确释放 | `FileDrop.jsx:88-91` |
| 前端API请求带超时和AbortController | `api.js:11-13` |

### 优先修复建议（后端部分）

1. **立即**：吊销 `.env.cloud` 中的DashScope API密钥并重新生成
2. **立即**：为 `/login` 和 `/register` 添加速率限制（建议5次/分钟/IP）
3. **短期**：考虑将诊断和对话端点改为 `requireAuth`
4. **短期**：将JSON body限制从20MB降至1MB
5. **短期**：为 `/health` 和 `/metrics` 添加IP白名单或基本认证
6. **中期**：将JWT令牌迁移至httpOnly Cookie
7. **中期**：添加文件magic bytes校验
8. **中期**：Chat的 `result` 字段从 `passthrough()` 改为白名单提取
9. **长期**：增加测试覆盖率，特别是安全相关场景
10. **长期**：考虑数据库加密或数据保留策略

---

## 八、核心功能问题：多模态识别未真正影响辨识结果

> 这是用户明确反馈的问题："七日方案和辨识是否能基于多模态识别后给出相应的答案，不能每个选项给的都一样"

### 问题概述

系统设计了两种推理模式：
- **public-free（默认）**：浏览器轻量特征 + 规则引擎
- **offline-qwen**：Qwen3-VL多模态识别 + 规则引擎

但在默认 public-free 模式下，**上传不同图片几乎不影响辨识结果和七日方案**，系统实质是"症状驱动的规则引擎"，图像识别只是展示层面的装饰。

### 数据流追踪

```
前端提交
  ├─ public-free（默认）：
  │    FormData = { symptoms, profile, hour, browserFeatures }
  │    ★ 不发送图片文件 ★（AssessmentPage.jsx 第124行条件控制）
  │    browserFeatures = Canvas像素统计（亮度/锐度/色调桶）
  │
  └─ offline-qwen：
       FormData = { ...同上, tongue?, face?, palm? }
       ★ 才会append图片文件 ★

后端处理（analysisService.js）
  ├─ 仅 offline-qwen 模式才调用 analyzeWithQwenVision（第621行）
  │    → base64编码图片 → POST到Qwen3-VL → 解析JSON特征
  │    → 但.env指向127.0.0.1:8000，云端环境无法连通 → 回退规则
  │
  └─ public-free 模式：
       → browserFeatures.featureText 如"舌像，光线可用，纹理清晰，色调偏红"
       → applyVisionTextScores 正则匹配"舌红|少津|黄腻|暗沉"等TCM术语
       → ★ 但浏览器特征文本与正则关键词几乎不匹配 ★
       → 体质打分实际由症状主导
```

### 具体问题

#### 1. 默认模式不上传图片到后端

**文件**：`jetlag-frontend/src/components/BookExperience.jsx` 问诊页提交逻辑

前端仅在 `inferenceMode === 'offline-qwen'` 时才将图片文件 append 到 FormData。默认 `public-free` 模式下，图片在浏览器端经 Canvas 统计后被丢弃，后端永远看不到原始图片。

#### 2. 浏览器"识别"只是像素统计，非多模态识别

**文件**：`jetlag-frontend/src/inference/browserVision.js`

`analyzeBrowserImage()` 的实际行为：
- 取图片中心 96×96 像素区域
- 计算平均亮度、锐度（相邻像素灰度差）、平均RGB
- 分类为离散桶：`光线偏暗/偏亮/可用`、`清晰度偏低/纹理清晰/可用`、`色调偏红/偏暗/偏淡/平和`
- `modelBacked: false`（第121行）明确标记这不是模型识别
- **无法识别舌色、舌苔、面色、黑眼圈等任何中医特征**

#### 3. 浏览器特征文本与后端正则关键词几乎不匹配

**文件**：`jetlag-backend/src/services/analysisService.js` 第200-225行 `applyVisionTextScores`

后端正则匹配的TCM关键词：`舌红|红点|少苔|少津|黄腻|白腻|暗沉|淡白|裂纹|齿痕|面白|面黄|黑眼圈|掌红` 等

浏览器 `featureText` 输出格式：`"舌像，光线可用，纹理清晰，色调偏红"`

逐一核对结果：

| 浏览器特征文本 | 后端正则 | 是否匹配 |
|---|---|---|
| "色调偏红" | `舌红` | ❌ 不匹配 |
| "色调偏暗" | `暗沉` | ❌ 不匹配 |
| "色调偏淡" | `淡白` | ❌ 不匹配 |
| "面部暗沉倾向" | `暗沉` | ✅ 匹配（唯一有效） |
| "面部暖色倾向" | `面黄` | ❌ 不匹配 |
| "面色偏淡" | `面白` | ❌ 不匹配 |
| "掌色偏红" | `掌红` | ❌ 不匹配 |
| "掌色偏淡" | （无对应正则） | ❌ 不匹配 |

**结论**：14种浏览器特征输出中，仅"面部暗沉倾向"1种能被后端正则匹配，视觉特征对体质打分的影响接近于零。

#### 4. offline-qwen模式理论可用但实际不通

**文件**：`jetlag-backend/.env`

```
OPEN_MODEL_BASE_URL=http://127.0.0.1:8000/v1
OPEN_MODEL_API_KEY=（空）
```

- `127.0.0.1:8000` 在HuggingFace Space云部署环境中无法连通
- 需要本地运行Qwen3-VL服务才能使用
- `.env.cloud` 中有DashScope API密钥和 `https://dashscope.aliyuncs.com/compatible-mode/v1` baseURL，但未确认是否在云端部署时使用

#### 5. 七日方案差异化来源

**文件**：`jetlag-backend/src/services/analysisService.js` 第295-387行 `buildAdaptiveFocus`

七日方案确实是动态拼接的（非纯固定模板），但差异化来源：

| 输入变化 | 输出是否变化 | 说明 |
|---|---|---|
| 不同症状 | ✅ 变化 | 症状是体质打分和方案的主驱动力 |
| 不同图片（public-free） | ❌ 几乎不变 | 图片像素不被分析，浏览器特征不匹配正则 |
| 不同图片（offline-qwen，Qwen可用） | ✅ 变化 | Qwen真实特征影响打分 |
| 有图片 vs 无图片 | ⚠️ 微弱变化 | observation布尔值给固定体质+1，通常不改变排序 |
| 不同profile（年龄/作息） | ✅ 变化 | 影响睡眠建议和运动强度修饰语 |
| 不同时辰 | ✅ 变化 | 影响当前经络和immediateActions |

### 需要你决策的修复方案

以下方案可单独或组合使用，需要你决定：

#### 方案A：修复正则匹配（最小改动，立即可做）

将 `applyVisionTextScores` 的正则关键词与浏览器特征文本对齐：
- `色调偏红` → 匹配 `偏红` → yinDeficiency +1
- `色调偏暗` → 匹配 `偏暗` → bloodStasis +1
- `色调偏淡` → 匹配 `偏淡` → qiDeficiency +1
- `面部暗沉倾向` → 已匹配 `暗沉` → kidneyEssence +1
- `面色偏淡` → 匹配 `偏淡` → qiBloodDeficiency +1
- `掌色偏红` → 匹配 `偏红` → dampHeat +1

**优点**：改动小，立即让浏览器特征影响体质判断
**缺点**：仍然是像素统计，不是真正的多模态识别，精度有限

#### 方案B：默认模式也上传图片到后端

修改前端提交逻辑，让 public-free 模式也 append 图片文件，后端用浏览器特征+图片文件做更深入分析。

**优点**：后端能看到原始图片
**缺点**：增加网络传输和服务器负载，与"图片不上传"的隐私设计冲突

#### 方案C：启用云端Qwen API

将后端环境变量改为使用 `.env.cloud` 中的DashScope API：
```
OPEN_MODEL_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPEN_MODEL_API_KEY=<已吊销>
```
并让默认模式也调用Qwen（或新增第三个模式 `cloud-qwen`）。

**优点**：真正的多模态识别，结果真正差异化
**缺点**：产生API费用，需要处理密钥安全，增加响应延迟

#### 方案D：前端接入Transformers.js本地模型

按技术文档第3章的设计，在浏览器中用ONNX Runtime Web / Transformers.js加载轻量视觉模型。

**优点**：真正的本地多模态识别，无API费用
**缺点**：开发量大，模型下载体积大，兼容性问题

#### 方案E：增强浏览器特征提取（中间方案）

在 `browserVision.js` 中增加更多中医相关的像素分析：
- 舌象：HSV色彩空间分析舌色（淡白/红/绛/青紫）
- 舌苔：边缘vs中心亮度差估算舌苔厚度
- 面相：黑眼圈检测（眼部区域暗度）
- 手相：掌纹密度分析

**优点**：不依赖外部API，隐私友好
**缺点**：精度远不如真模型，开发量中等

### 我的建议

如果目标是"答辩演示时不同图片给不同结果"，**方案A（修复正则）+ 方案E（增强特征）** 组合最实际——改动小、不花钱、立即见效。如果答辩需要展示"真正的AI多模态识别"，则需 **方案C（云端Qwen）**。
