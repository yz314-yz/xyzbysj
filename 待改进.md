# 待改进清单 - 中医养生辅助系统

> 生成时间：2026-06-28（整合两次全面扫描结果）
> 扫描范围：前端 (jetlag-frontend/src/)、后端 (jetlag-backend/src/)、部署配置、Docker、CI/CD、毕业设计学术规范

---

## 总体评价

项目在**工程化基础**（Docker、CI/CD、安全头、限流、错误处理）上已达到相当不错的水平，远超一般本科毕业设计的平均工程水准。但在**毕业设计学术规范文档**、**数据持久化**、**用户体系**、**测试**、**API 文档**这五个维度上存在明显缺失，这些恰恰是答辩老师重点考察的部分。

---

## 一、P0 - 答辩必补（学术规范与核心功能）

### 1. 毕业设计文档缺失（最关键）

| # | 缺失文档 | 建议路径 | 说明 |
|---|----------|----------|------|
| 1.1 | 根目录 README.md | `README.md` | 项目介绍、技术栈、运行方式、目录结构、截图 |
| 1.2 | 需求分析文档 | `docs/需求分析.md` | 功能/非功能需求列表、用例图 |
| 1.3 | 系统设计文档 | `docs/系统设计.md` | 架构图、模块设计、接口设计、数据流图、类图/时序图 |
| 1.4 | 数据库设计文档 | `docs/数据库设计.md` | ER 图、表结构设计、数据字典（即使不实现也要有设计） |
| 1.5 | 测试报告 | `docs/测试报告.md` | 测试用例表、功能测试结果、测试结论、覆盖率 |
| 1.6 | 用户手册 | `docs/用户手册.md` | 操作步骤、截图、FAQ |

---

### 2. 数据库（最大硬伤）

- **现状**: 完全无数据库，无 Sequelize/Mongoose/SQLite/MySQL 依赖
- **问题**: 症状、体质、七日计划全部硬编码在 `tcmKnowledge.js`；诊断结果不持久化，返回后即丢失
- **建议**: 引入 SQLite（最轻量）或 MySQL，持久化诊断记录、用户、历史方案
- **位置**: `jetlag-backend/src/tcmKnowledge.js`（硬编码知识库）、`jetlag-backend/src/server.js`（无持久化）

---

### 3. 用户认证体系

- **现状**: 无登录注册、无 JWT/Session/Passport，任何人访问都是匿名
- **问题**: 无法区分用户，无法实现历史记录、个性化方案等功能
- **建议**: 引入 JWT 最简认证，为历史记录功能铺路
- **位置**: `jetlag-backend/src/server.js`（无 auth 中间件）

---

### 4. 测试完全缺失

- **现状**: 无 `test/`、`__tests__/`、`*.test.js`、`*.spec.jsx` 任何文件
- **问题**: `package.json` 的 `test` 脚本只是 `node --check`（语法检查）和 `npm run build`，不是真正的测试
- **建议**:
  - 后端：Jest + Supertest 测试 API 接口
  - 前端：Vitest 测试组件
- **位置**: `jetlag-backend/package.json`、`jetlag-frontend/package.json`

---

## 二、P1 - 强烈建议补（功能完整性）

### 5. 前端缺失功能

#### 5.1 无路由系统
- **问题**: 无 react-router，整个应用是单页滚动 + `IntersectionObserver` 高亮侧边栏
- **建议**: 引入 react-router，实现首页/采集/历史/关于等多路由
- **位置**: `jetlag-frontend/src/main.jsx`

#### 5.2 无数据导出功能
- **问题**: 七日计划、体质评估结果无法导出为 PDF/图片/Excel
- **建议**: 使用 html2canvas + jsPDF 实现导出
- **位置**: `jetlag-frontend/src/main.jsx`

#### 5.3 无历史记录查看
- **问题**: 无诊断历史查看功能（因为后端无数据库，结果一次性消费）
- **建议**: 数据库 + 用户登录后，可查看以往体质评估和七日计划

#### 5.4 无 toast/通知组件
- **问题**: 仅有内联 `form-error` 文案，无 toast/通知组件，错误反馈不够醒目
- **建议**: 引入 react-hot-toast 或自实现 toast 组件
- **位置**: `jetlag-frontend/src/main.jsx`

#### 5.5 组件未拆分
- **问题**: `main.jsx` 单文件 639 行，`FileDrop`、`VisionPanel`、`App`、所有常量全堆在一起
- **建议**: 拆分到 `components/`、`constants/`、`hooks/`、`services/` 目录
- **位置**: `jetlag-frontend/src/main.jsx`

#### 5.6 无主题切换
- **问题**: 无暗色模式/主题切换。CSS 已用 `:root` 变量（第 7-12 行），具备改造基础但未实现
- **建议**: 实现明暗主题切换
- **位置**: `jetlag-frontend/src/styles.css`

#### 5.7 无新手引导/帮助页
- **问题**: 无新手引导、无帮助页、无关于页
- **建议**: 添加使用引导和关于页面

#### 5.8 无国际化
- **问题**: 全部中文硬编码，无 i18n 框架
- **说明**: 对中医主题毕业设计可接受，但算缺失项

---

### 6. 后端缺失功能

#### 6.1 无 API 文档
- **问题**: 无 Swagger/OpenAPI、无 JSDoc
- **建议**: 引入 swagger-jsdoc 自动生成 API 文档
- **位置**: `jetlag-backend/src/server.js`

#### 6.2 无日志库
- **问题**: 有自定义 `logRequest` 输出到 `console.info`，但无 winston/morgan/pino，无日志分级、无文件落盘、无日志轮转
- **建议**: 引入 winston 落盘 + 按天轮转
- **位置**: `jetlag-backend/src/server.js` 第 70-84 行

#### 6.3 无定时任务
- **问题**: 无 node-cron/agenda。子午流注本可做"时辰提醒"定时推送
- **建议**: 引入 node-cron 实现时辰养生提醒

#### 6.4 无业务逻辑分层
- **问题**: `server.js` 路由+业务+配置混在一起，无 controller/service/model 分层
- **建议**: 拆分为 controller/service/model 三层架构
- **位置**: `jetlag-backend/src/server.js`（550 行）

#### 6.5 无文件持久化存储
- **问题**: multer 上传到本地 `uploads/`，请求结束即删。无 OSS/S3/MinIO 集成
- **建议**: 如需保留望诊图片，引入对象存储

#### 6.6 无输入校验库
- **问题**: 有自定义 `validateDiagnoseBody`，但无 joi/zod，校验逻辑手写
- **建议**: 引入 zod 或 joi 做请求体校验
- **位置**: `jetlag-backend/src/server.js` 第 198-235 行

#### 6.7 无邮件/通知服务
- **问题**: 无 nodemailer，无推送服务
- **建议**: 如需养生提醒推送，引入 nodemailer

---

## 三、P2 - 工程质量提升

### 7. 安全问题

#### 7.1 CORS 默认全开放
- **文件**: `jetlag-backend/src/server.js` 第 22-30 行
- **问题**: `CORS_ORIGIN` 留空时允许所有来源访问，存在安全隐患
- **建议**: 留空时仅允许同源，或设置默认拒绝

#### 7.2 错误信息直接返回客户端
- **文件**: `jetlag-backend/src/server.js` 第 304-305 行
- **问题**: `res.status(500).json({ error: error.message })` 直接返回错误详情，可能泄露文件路径、堆栈等内部信息
- **建议**: 返回通用错误文案，详细错误仅记日志

#### 7.3 无速率限制
- **文件**: `jetlag-backend/src/server.js`
- **问题**: `/api/v1/diagnose` 接口会触发昂贵的 Qwen VL 调用，却无任何限流
- **建议**: 引入 `express-rate-limit`，限制每个 IP 每分钟请求次数

#### 7.4 上传文件无 MIME 校验
- **文件**: `jetlag-backend/src/server.js` 第 40-43 行
- **问题**: multer 仅限制 8MB 大小，未校验 MIME 类型
- **建议**: 添加文件类型白名单校验（仅允许 image/jpeg、image/png 等）

#### 7.5 无安全响应头
- **文件**: `jetlag-backend/src/server.js`
- **问题**: 未使用 `helmet`，缺少 CSP、X-Frame-Options 等基础安全头
- **建议**: 引入 `helmet` 中间件

---

### 8. 功能性 Bug

#### 8.1 前后端症状列表重复定义
- **文件**: `jetlag-frontend/src/main.jsx` 第 32-47 行 vs `jetlag-backend/src/tcmKnowledge.js` 第 1-16 行
- **问题**: 前端硬编码了 `symptomOptions`，后端有 `GET /api/v1/symptoms` 接口但前端从未调用
- **建议**: 前端启动时调用后端接口拉取症状列表

#### 8.2 提交无前端校验
- **文件**: `jetlag-frontend/src/main.jsx` 第 196-219 行
- **问题**: 即使未选任何症状、未传任何图片，"生成七日调理计划"按钮仍可点击提交
- **建议**: 至少要求选择 1 个症状或上传 1 张图片才能提交

#### 8.3 `hour=0`（子时）潜在 Bug
- **文件**: `jetlag-backend/src/server.js` 第 288 行
- **问题**: `req.body.hour || new Date().getHours()` 中，午夜 0 点时 `0` 是 falsy，会被替换成当前小时
- **建议**: 改用 `??` 运算符：`req.body.hour ?? new Date().getHours()`

#### 8.4 后端 `meridian.range` 字段未渲染
- **文件**: `jetlag-frontend/src/main.jsx` 第 294-297 行
- **问题**: 后端返回 `result.meridian = { range, name, meridian, advice }`，但前端只渲染 `immediateActions`，时间区间从未展示
- **建议**: 在子午流注区块展示时辰/经脉/时间区间

---

### 9. 后端代码改进

#### 9.1 Qwen 调用无超时
- **文件**: `jetlag-backend/src/qwenVision.js` 第 95-105 行
- **问题**: `client.chat.completions.create` 未设 `timeout`，模型服务挂起时整个请求链路阻塞
- **建议**: 添加 `timeout: 30000` 选项

#### 9.2 同步阻塞读取大文件
- **文件**: `jetlag-backend/src/qwenVision.js` 第 30 行
- **问题**: `fs.readFileSync(file.path, 'base64')` 同步读取，3 张 8MB 图片会阻塞事件循环
- **建议**: 改用 `fs.promises.readFile`

#### 9.3 Qwen 响应未校验
- **文件**: `jetlag-backend/src/qwenVision.js` 第 107 行
- **问题**: 直接信任 `response.choices?.[0]?.message?.content`，未检查 `response.error`
- **建议**: 添加响应状态和结构校验

#### 9.4 JSON 提取脆弱
- **文件**: `jetlag-backend/src/qwenVision.js` 第 40-53 行
- **问题**: `extractJsonObject` 用贪婪正则匹配，模型输出含多段 JSON 时会提取错误
- **建议**: 使用非贪婪匹配或更健壮的 JSON 提取逻辑

#### 9.5 cleanup 静默吞错
- **文件**: `jetlag-backend/src/server.js` 第 250-258 行
- **问题**: `fs.unlinkSync` 失败时完全静默
- **建议**: 至少 `console.warn` 记录失败

#### 9.6 无全局错误处理中间件
- **文件**: `jetlag-backend/src/server.js`
- **问题**: 缺少 Express 错误处理中间件，multer 上传超限等错误格式不统一
- **建议**: 添加 `app.use((err, req, res, next) => ...)` 兜底

#### 9.7 响应内嵌配置文档
- **文件**: `jetlag-backend/src/server.js` 第 238-246 行
- **问题**: `modelSuggestion` 字段把环境变量配置示例塞进每次 diagnose 响应
- **建议**: 配置文档放前端静态展示，不作为运行时数据返回

#### 9.8 `buildAnalysis` 重复调用
- **文件**: `jetlag-backend/src/server.js` 第 290、301 行
- **问题**: 同一请求内调用两次 `buildAnalysis`，第一次仅用于喂给 Qwen prompt
- **建议**: 拆分出"初步方向"轻量计算，减少重复打分

#### 9.9 `/health` 不检查上游
- **文件**: `jetlag-backend/src/server.js` 第 260-271 行
- **问题**: 健康检查只报告本地状态，不探测 Qwen VL 服务可达性
- **建议**: 增加上游服务可达性探测

---

### 10. 前端代码改进

#### 10.1 fetch 无超时与取消
- **文件**: `jetlag-frontend/src/main.jsx` 第 206-210 行
- **问题**: 未使用 `AbortController`，后端挂起时 loading 状态无限持续
- **建议**: 添加超时取消机制

#### 10.2 profile 输入框类型缺失
- **文件**: `jetlag-frontend/src/main.jsx` 第 258-267 行
- **问题**: 所有 `<input>` 均未指定 `type`
  - `age` 应为 `type="number"`（加 min/max）
  - `bedtime`/`wakeTime` 应为 `type="time"`
  - `gender` 应改为 `<select>`
- **建议**: 根据字段语义设置正确的 input 类型

#### 10.3 上传图片无缩略图预览
- **文件**: `jetlag-frontend/src/main.jsx` 第 92-102 行
- **问题**: 作为"望诊采集"系统，上传后仅显示文件名，无图片预览
- **建议**: 上传后显示缩略图

#### 10.4 无法单独移除已选文件
- **文件**: `jetlag-frontend/src/main.jsx` 第 92-102 行
- **问题**: 只有"清空表单"按钮能重置全部，无法删除单张图片
- **建议**: 每张图片添加删除按钮

#### 10.5 文件输入 value 未重置
- **文件**: `jetlag-frontend/src/main.jsx` 第 95 行
- **问题**: 原生 `<input type="file">` 的 value 不会被 `resetForm` 清空，重新选择同一文件时 `onChange` 不触发
- **建议**: resetForm 时手动清空 `e.target.value = ''`

#### 10.6 catch 块提示文案误导
- **文件**: `jetlag-frontend/src/main.jsx` 第 211-215 行
- **问题**: 错误提示写"展示本地演示结果"，但实际 `result` 是空占位状态
- **建议**: 修正提示文案，或实现真正的本地规则兜底

#### 10.7 前端硬编码模型名
- **文件**: `jetlag-frontend/src/main.jsx` 第 60 行
- **问题**: `emptyResult.engineStatus.vision.model` 写死 `'Qwen/Qwen3-VL-8B-Instruct'`
- **建议**: 从后端 /health 接口动态获取

#### 10.8 FileDrop 命名与实现不符
- **文件**: `jetlag-frontend/src/main.jsx` 第 92-102 行
- **问题**: 组件名为 `FileDrop` 但仅支持点击选择，无拖拽上传
- **建议**: 添加拖拽上传支持，或改名为 `FilePicker`

#### 10.9 无 ErrorBoundary
- **文件**: `jetlag-frontend/src/main.jsx`
- **问题**: 任意渲染错误会导致整页白屏
- **建议**: 添加 React ErrorBoundary 组件

---

### 11. 部署配置改进

#### 11.1 三个 Dockerfile 均以 root 运行
- **文件**: `Dockerfile`、`jetlag-frontend/Dockerfile`、`jetlag-backend/Dockerfile`
- **问题**: 无 `USER` 指令，容器以 root 运行，违反最小权限原则
- **建议**: 创建非 root 用户运行应用

#### 11.2 无 HEALTHCHECK
- **文件**: 三个 Dockerfile
- **问题**: 未设置 `HEALTHCHECK`，编排器无法探活
- **建议**: 添加 `HEALTHCHECK CMD curl -f http://localhost:PORT/health || exit 1`

#### 11.3 GHCR 镜像名大小写隐患
- **文件**: `.github/workflows/ghcr-fullstack.yml` 第 14 行
- **问题**: `IMAGE_NAME` 使用 `github.repository_owner` 和 `github.event.repository.name`，若含大写字母会导致 GHCR 推送失败
- **建议**: 通过 step 将镜像名转小写

#### 11.4 HF Space 硬编码镜像地址
- **文件**: `deploy/huggingface-space/Dockerfile` 第 3 行
- **问题**: `FROM ghcr.io/yz314-yz/xyzbysj:latest` 写死了用户名和仓库名，仓库改名后会失效
- **建议**: 文档化替换步骤，或改为构建参数

#### 11.5 单一 `:latest` tag 无法回滚
- **文件**: `.github/workflows/ghcr-fullstack.yml` 第 14、40 行
- **问题**: 只有 `:latest` 标签，一旦推送错误版本无法回滚
- **建议**: 增加 git SHA 或 semver 标签

#### 11.6 GitHub Actions 无缓存
- **文件**: `.github/workflows/ghcr-fullstack.yml` 第 34-40 行
- **问题**: 未配置 `cache-from`/`cache-to`，每次全量构建
- **建议**: 添加 Docker 层缓存

#### 11.7 GitHub Actions 无测试步骤
- **文件**: `.github/workflows/ghcr-fullstack.yml`
- **问题**: 构建前无 lint/test，问题代码可直接进镜像
- **建议**: 添加 `npm run lint` 和 `npm test` 步骤

#### 11.8 entrypoint 未转义 env 值
- **文件**: `jetlag-frontend/docker-entrypoint.sh` 第 4-8 行
- **问题**: `${VITE_API_BASE:-}` 未转义，含特殊字符会破坏生成的 JS
- **建议**: 对值做 JSON 转义

#### 11.9 前端构建依赖未分类
- **文件**: `jetlag-frontend/package.json` 第 11-18 行
- **问题**: `vite`、`@vitejs/plugin-react` 放在 `dependencies` 而非 `devDependencies`
- **建议**: 移到 `devDependencies`

#### 11.10 后端缺开发工具
- **文件**: `jetlag-backend/package.json`
- **问题**: 无 `devDependencies`，`dev` 脚本与 `start` 相同，无热重载
- **建议**: 引入 `nodemon`，添加 `lint`/`test` 脚本

---

## 四、P3 - 锦上添花（低优先级）

### 12. 代码质量

#### 12.1 CSS 滥用 `!important`
- **文件**: `jetlag-frontend/src/styles.css` 第 157 行
- **建议**: 通过提高选择器特异性解决

#### 12.2 无 CSS 变量
- **文件**: `jetlag-frontend/src/styles.css`
- **问题**: `#275b45`、`#2f6f54` 等颜色反复出现
- **建议**: 使用 `:root` 自定义属性统一管理

#### 12.3 评分环颜色固定
- **文件**: `jetlag-frontend/src/styles.css` 第 150-156 行
- **问题**: `.score-ring` 边框颜色固定，不随 confidence 变化
- **建议**: 低分显示警示色

#### 12.4 无 ESLint/Prettier 配置
- **文件**: `jetlag-frontend/package.json`、`jetlag-backend/package.json`
- **建议**: 添加代码质量工具

#### 12.5 字符串作为 React key
- **文件**: `jetlag-frontend/src/main.jsx` 第 290、296 行
- **建议**: 改用 index 或稳定 id

#### 12.6 按钮缺 `type="button"`
- **文件**: `jetlag-frontend/src/main.jsx` 第 246、274、279 行
- **建议**: 显式声明 `type="button"`

#### 12.7 无 `engines` 字段
- **文件**: 两个 `package.json`
- **建议**: 声明 Node 版本要求

#### 12.8 无 `browserslist` 配置
- **文件**: `jetlag-frontend/package.json`
- **建议**: 添加浏览器兼容性声明

#### 12.9 .dockerignore / .gitignore 残留 Flutter 条目
- **文件**: `.dockerignore`、`.gitignore`
- **问题**: 残留 `.dart_tool/`、`flutter-sdk/`、`android/`、`ios/`、`lib/generated_plugin_registrant.dart` 等条目
- **建议**: 清理无关条目

#### 12.10 前端 .dockerignore 缺少 .env
- **文件**: `jetlag-frontend/.dockerignore`
- **建议**: 添加 `.env`、`.env.local` 排除

#### 12.11 文件输入 `display:none` 破坏 a11y
- **文件**: `jetlag-frontend/src/styles.css` 第 117 行
- **问题**: `.drop input { display: none; }` 把文件输入从无障碍树中完全移除
- **建议**: 改用 visually-hidden 技巧（`position:absolute; clip:rect(0,0,0,0)` 等）

#### 12.12 `emptyResult` 共享引用
- **文件**: `jetlag-frontend/src/main.jsx` 第 49-87、157、193 行
- **问题**: `useState(emptyResult)` 与 `setResult(emptyResult)` 直接使用模块级常量引用
- **建议**: 使用工厂函数 `createEmptyResult()` 避免引用污染

#### 12.13 `parseJsonField` 过度宽容
- **文件**: `jetlag-backend/src/server.js` 第 50-62 行
- **问题**: 非 JSON 字符串会被逗号分割成数组，可能产生预期外的数据形状

#### 12.14 `OPEN_MODEL_API_KEY` 默认 'EMPTY'
- **文件**: `jetlag-backend/src/qwenVision.js` 第 25 行
- **问题**: 若误指向真实 OpenAI 端点，会发送 'EMPTY' 作为密钥
- **建议**: 启动时检测并日志提醒

---

## 五、DevOps 缺失

| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| 13.1 | 开发环境配置 | ✅ 已有 | `.env.example`、`.env.cloud.example`、`docker-compose.cloud.yml` |
| 13.2 | docker-compose | ✅ 已有 | 前后端 + 模型服务 |
| 13.3 | CI/CD 流水线 | ✅ 已有 | GHCR 构建推送，含多 tag + 缓存 |
| 13.4 | 容器安全 | ✅ 已有 | 非 root 用户运行 |
| 13.5 | HEALTHCHECK | ✅ 已有 | 三个 Dockerfile 均配置 |
| 13.6 | 监控 | ❌ 缺失 | 无 Prometheus/Grafana，无健康指标采集 |
| 13.7 | 日志收集 | ❌ 缺失 | 无 ELK/Loki，日志仅 console 输出 |
| 13.8 | CI 测试步骤 | ⚠️ 部分 | 有 `npm test` 但只是语法检查，非真正测试 |

---

## 六、改进顺序建议

```
第1步：P0 - 答辩必补
  ├─ 编写 6 份学术文档（README、需求分析、系统设计、数据库设计、测试报告、用户手册）
  ├─ 引入 SQLite 数据库
  ├─ 实现用户登录注册（JWT）
  └─ 编写真正的单元测试
       ↓
第2步：P1 - 功能完整性
  ├─ 引入 react-router 路由
  ├─ 实现诊断历史记录
  ├─ 实现数据导出（PDF/图片）
  ├─ 添加 API 文档（Swagger）
  ├─ 添加 toast 通知组件
  ├─ 组件拆分
  └─ 后端业务分层（controller/service/model）
       ↓
第3步：P2 - 工程质量
  ├─ 修复安全问题（CORS、错误脱敏、限流、MIME 校验、helmet）
  ├─ 修复功能性 Bug（hour=0、前端校验、症状列表同步、meridian 渲染）
  ├─ 优化后端（Qwen 超时、异步读文件、响应校验、错误中间件）
  ├─ 优化部署（Dockerfile 安全、CI 缓存、多 tag、HEALTHCHECK）
  └─ 引入日志库 winston
       ↓
第4步：P3 - 锦上添花
  ├─ 主题切换
  ├─ ESLint/Prettier
  ├─ CSS 变量重构
  ├─ 新手引导
  ├─ 监控（Prometheus）
  └─ 国际化
```

---

## 七、环境变量配置对照表

### 后端必需环境变量

| 变量名 | 说明 | 示例值 |
|--------|------|--------|
| `PORT` | 服务端口 | `7860` |
| `PUBLIC_API_BASE` | 前端 API 地址（同源留空） | （空） |
| `CORS_ORIGIN` | 允许的前端来源（逗号分隔） | `https://xxx.hf.space` |
| `OPEN_MODEL_BASE_URL` | Qwen3-VL 服务地址 | `https://api.siliconflow.cn/v1` |
| `OPEN_MODEL_API_KEY` | 模型 API 密钥 | `sk-xxxxxxxx` |
| `OPEN_MODEL_NAME` | 模型名称 | `Qwen/Qwen3-VL-8B-Instruct` |
| `NODE_ENV` | 运行环境 | `production` |

### 前端环境变量

| 变量名 | 说明 | 示例值 |
|--------|------|--------|
| `VITE_API_BASE` | 后端 API 地址（同源留空） | （空） |

---

## 八、待确认事项

- [ ] 是否需要引入数据库（当前无持久化存储）→ **强烈建议是**
- [ ] 是否需要用户登录系统 → **强烈建议是**
- [ ] 是否需要历史记录功能 → **强烈建议是**
- [ ] Qwen3-VL 服务是否稳定（硅基流动免费额度）
- [ ] 是否需要 Deno Deploy 反向代理（中国大陆访问优化）
- [ ] 是否需要移动端适配优化
- [ ] 是否需要国际化
- [ ] 是否需要监控告警系统

---

## 九、一句话总结

> **工程实现已属优秀（安全、部署、CI/CD、错误处理均到位），但作为毕业设计，最大的短板是"无数据库、无用户体系、无测试、无学术文档"这"四无"。建议优先补齐 P0 的 6 份文档和数据库+用户体系+测试，这是答辩老师最看重的部分。**

---

## 十、问题统计

| 优先级 | 数量 | 说明 |
|--------|------|------|
| P0（答辩必补） | 4 大项 | 学术文档、数据库、用户认证、测试 |
| P1（强烈建议） | 14 项 | 前端 8 项 + 后端 7 项 |
| P2（工程质量） | 28 项 | 安全 5 + Bug 4 + 后端 9 + 前端 9 + 部署 10 |
| P3（锦上添花） | 14 项 | 代码质量 14 |
| **总计** | **60 项** | |
