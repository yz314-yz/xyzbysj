jetlag-sync/
├── jetlag-backend/           # 后端 Node.js 项目
│   ├── src/
│   │   ├── controllers/      # 业务逻辑（AI诊断、天气聚合）
│   │   ├── routes/           # 接口路由
│   │   └── server.js         # 后端入口文件
│   ├── uploads/              # 临时存放安卓上传的图片
│   ├── .env                  # 环境变量（存放 DeepSeek API Key）
│   └── package.json
└── jetlag-frontend/          # 前端 Flutter 项目
    ├── android/
    ├── lib/
    │   ├── models/           # 数据模型
    │   ├── services/         # API 请求、相机、传感器服务
    │   ├── views/            # 苹果风 UI 界面 (Dashboard, Camera, Report)
    │   └── main.dart         # 前端入口
    └── pubspec.yaml          # 依赖管理